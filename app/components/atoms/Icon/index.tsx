// import React from "react";
// import "./style.less";

// interface IProps {
//   alt: string;
//   className?: any;
//   src: any;
//   onClick?: React.MouseEventHandler<HTMLElement>;
//   border: boolean;
// }

// const Icon = (props: IProps) => {
//   const { alt, className, src, border, onClick } = props;

//   return (
//     <div className="icons">
//       {border ? (
//         <div className="border-icon">
//           <img alt={alt} className={className} onClick={onClick} src={src} />
//         </div>
//       ) : (
//         <img alt={alt} className={className} onClick={onClick} src={src} />
//       )}
//     </div>
//   );
// };

// export default Icon;
