// import React from 'react';

// import './style.less';

// interface IProps {
//     id: string;
//     containerStyle?: React.CSSProperties;
//     children: React.ReactElement;
//     className?: string;
// }

// const SectionCard = (props: IProps): React.ReactElement => {
//     const { id, containerStyle, children, className } = props;
//     return (
//         <div key={id} style={containerStyle} className={`section_card_container ${className}`}>
//             {children}
//         </div>
//     )
// }

// export default SectionCard;
