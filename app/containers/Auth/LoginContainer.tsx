// import Login from '@presentation/auth/Login/index';
// import { IState } from '@store/webInterfaces';
// import { connect } from 'react-redux';

// const mapDispatchToProps = (dispatch: any) => {
//   return {
//       // login: (payload) => dispatch(login(payload)),
//       // checkUserExist: (payload) => dispatch(checkUserExist(payload))
//   }
// }

// const mapStateToProps = (state: IState) => ({
//   // loginData: loginSelector(state),
//   // loginLoader: loginLoaderSelector(state),
//   // loginError: loginErrorSelector(state),

//   // checkUserExistData: checkUserExistSelector(state),
//   // checkUserExistError: checkUserExistErrorSelector(state),
// })


// export default connect(mapStateToProps, mapDispatchToProps)(Login);
